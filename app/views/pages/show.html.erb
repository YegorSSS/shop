<% provide(:title, @page.title) %>
<% provide(:keywords, @page.keyword) %>
<% provide(:description, @page.description) %>

<div class="kroshka">
	<a href="/">Главная</a> / <span><%= @page.title %></span>
</div>
<div class="content-txt">
<h1><%= @page.title %></h1>
<%= @page.content %>
</div>